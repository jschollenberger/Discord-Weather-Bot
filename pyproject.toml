[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# F (pyflakes) catches real bugs: undefined names, unused imports, etc.
# E9 catches syntax/indentation errors.  B (flake8-bugbear) catches likely
# bugs such as mutable default arguments.  The codebase deliberately uses a
# compact `if x: return y` style, so E701/E702 are not selected.
select = ["F", "E9", "E722", "E731", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
